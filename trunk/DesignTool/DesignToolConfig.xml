<?xml version="1.0" encoding="utf-8" ?>
<data>

	<!-- The default CiF Library to load. -->
	<!-- Backslashes (\) for Windows paths, foreslashes (/) for OS X and linux paths. -->
	<!-- Please use full path names as AIR doesn't handle application-space relative paths in directories above the app's directory. -->
	<State>
		
	</State>
	<Library>
		
	</Library>
	
	<Scale>
		1.0
	</Scale>
	
	<!-- Default authoring view of the tool: microtheory, socialgame, rulesearch, or scenario. -->
	<AuthoringView>socialgame</AuthoringView>
	
</data>